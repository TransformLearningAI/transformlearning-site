export const metadata = {
  title: 'Request Access — Arrival',
  description: 'Request access to Arrival, the AI-powered adaptive learning system for higher education. Free pilot available for faculty teaching gateway courses.',
}

export default function AccessLayout({ children }) {
  return children
}
